using Pretender;

namespace Pretender
{
    /// <summary>
    /// 
    /// </summary>
    [AttributeUsage(AttributeTargets.Class)]
    public class PretendAttribute : Attribute
    {
        // TODO: Use this as a marker for instead of creating our own class
        // do it as a partial to this type
    }
}
